package com.ancientlightstudios.quarkus.kotlin.openapi.models.openapi.components

/**
 * Marker interface for components which contain references to other schemas. These schemas are also
 * important for the decision if this schema will produce a model type or not.
 */
interface ReferencingComponent