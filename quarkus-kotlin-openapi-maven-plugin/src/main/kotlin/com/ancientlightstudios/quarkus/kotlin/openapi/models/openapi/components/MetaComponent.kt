package com.ancientlightstudios.quarkus.kotlin.openapi.models.openapi.components

/**
 * Marker interface for components which don't define/alter the structure of a schema, but modify its behaviour.
 * Schemas with only these components will most certainly not produce a new model type.
 */
interface MetaComponent